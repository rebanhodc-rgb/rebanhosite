# Frontend

Camada de interface da loja REBANHO.

- `components/`: UI, marca, produto, carrinho, checkout e admin.
- `lib/`: utilitarios que rodam no navegador, como estado local do carrinho.

Paginas em `app/(public)` e `app/(admin)` importam componentes daqui, mantendo UI separada das regras de servidor.
